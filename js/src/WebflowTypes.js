// Typed models for the Webflow SDK (JSDoc typedefs).
//
// GENERATED from the API model: main.kit.entity.<e>.fields[] and per-op
// params (op.<name>.points[].args.params[]). Field/param types come from the
// canonical type sentinels via @voxgig/sdkgen canonToType (source of truth:
// @voxgig/apidef VALID_CANON). Annotations only — no runtime effect. Do not
// edit by hand.

/**
 * @typedef {Object} Collection
 * @property {string} [createdOn]
 * @property {string} [displayName]
 * @property {string} [id]
 * @property {string} [slug]
 */

/**
 * @typedef {Object} CollectionLoadMatch
 * @property {string} id
 */

/**
 * @typedef {Object} CollectionListMatch
 * @property {string} site_id
 */

/**
 * @typedef {Object} Item
 * @property {string} [cmsLocaleId]
 * @property {Object} [fieldData]
 * @property {string} [id]
 * @property {string} [lastPublished]
 * @property {string} [lastUpdated]
 */

/**
 * @typedef {Object} ItemLoadMatch
 * @property {string} collection_id
 * @property {string} id
 */

/**
 * @typedef {Object} ItemListMatch
 * @property {string} collection_id
 */

/**
 * @typedef {Object} Site
 * @property {string} [createdOn]
 * @property {string} [displayName]
 * @property {string} [id]
 * @property {string} [lastPublished]
 * @property {string} [shortName]
 */

/**
 * @typedef {Object} SiteLoadMatch
 * @property {string} id
 */

/**
 * @typedef {Object} SiteListMatch
 * @property {string} [createdOn]
 * @property {string} [displayName]
 * @property {string} [id]
 * @property {string} [lastPublished]
 * @property {string} [shortName]
 */

